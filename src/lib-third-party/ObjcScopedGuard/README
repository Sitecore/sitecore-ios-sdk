This is a scoped guard class in ObjectiveC++ that uses blocks to release resources
Scoped guard executes the block in its C++ destructor.

A scoped guard in ObjectiveC++ that uses blocks to release resources

License : BSD

